#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    int t;
    cin>>t;
    while (t>0) {
        int dummy;
        int n;
        cin>>n;
        vector<int> a;
        for (int i = 0 ; i<n ; i++) {
            cin>>dummy;
            a.push_back(dummy);
        }

        // Sort it
        // for (int )


        t--;
    }
}