#include <iostream>
#include <algorithm>

using namespace std;

bool CheckPalindrome(string &s) {
    int n = s.length();
    string reverseone;
    

}

int main() {
    string s = "";
    cout<<"Is Palindrome: "<<CheckPalindrome(s)<<endl;
}