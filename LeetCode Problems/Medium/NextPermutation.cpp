#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

void nextPermutation(vector<int>& nums) {
    // last sae shuru karna ha
    
}

int main() {
    vector<int> nums;
    nums = {1,2,3};
    nextPermutation(nums);
    nums = {3,2,1};
    nextPermutation(nums);
    nums = {1,1,5};
    nextPermutation(nums);
}